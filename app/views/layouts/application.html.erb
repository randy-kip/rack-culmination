<!DOCTYPE html>
<html>
  <head>
    <title>RailsAltogether</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header>
      <nav class="navbar">
        <div class="container">
          <span class="navbar-brand"><%= link_to "Green Harbour", root_path %></span>
          <ul class="navbar-nav">
            <li class="nav-item"><%= link_to 'Plants', plants_path, class: 'nav-link' %></li>
            <li class="nav-item"><%= link_to 'People', people_path, class: 'nav-link' %></li>
          </ul>
        </div>
      </nav>
    </header>

    <%= yield %>
  </body>
</html>
